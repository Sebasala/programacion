---
import { BASE_URL } from "@src/consts.js";
import Button from "@components/Button.astro";

const { text, path } = Astro.props;
---

<header>
	<nav>
		<Button text={text} href={`${BASE_URL}${path}`} />
	</nav>
</header>
<style>
	nav {
		margin: calc(var(--space) / 2) auto;
	}
</style>
